<%# アラーム一覧画面 %>
<div class="container mx-auto px-4 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900 mb-6">アラーム一覧</h1>
    
    <% if @alarms.present? %>
      <%# アラーム一覧が存在する場合 %>
      <%= link_to '新規アラーム作成', new_alarm_path, 
          class: "inline-block px-6 py-3 bg-gray-900 text-white rounded-lg hover:bg-gray-800 transition text-lg font-semibold mb-6" %>
      
      <%= render 'alarm_list', alarms: @alarms %>
    <% else %>
      <%# アラーム一覧が存在しない場合 %>
      <%= render 'no_alarms_message' %>
    <% end %>
  </div>
</div>
