<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold pb-4 border-b-2 border-emerald-500 mb-8">定型文一覧</h1>
  
  <div class="space-y-8">
    <% @message_templates.group_by(&:reason).each do |reason, templates| %>
      <section class="bg-white rounded-lg shadow-md p-6">
        <div class="mb-4">
          <h2 class="text-xl font-semibold text-gray-900">
            <%= reason %>
          </h2>
        </div>
        
        <ul class="space-y-3">
          <% templates.each do |template| %>
            <li class="px-4 py-3 bg-gray-100 rounded hover:bg-gray-200 transition">
              <p class="text-gray-900">
                <%= template.template %>
              </p>
            </li>
          <% end %>
        </ul>
      </section>
    <% end %>
  </div>
</div>
